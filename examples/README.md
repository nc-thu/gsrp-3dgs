# Small examples

The complete paper workload is intentionally not distributed. Use the package
tests and aggregate source data shipped with the simulator examples for
extractor-
free smoke checks. Real-model evaluation requires the user to provide a legal
model, dataset source, camera set, and renderer adapter.
