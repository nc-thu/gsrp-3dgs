# Small examples

The complete paper workload is intentionally not distributed. Use the package
tests and the aggregate source data under
`simulators/precision/results/profile_selection_v2/` for extractor-free smoke
checks. Real-model evaluation requires the user to provide a legal model,
dataset source, camera set, and renderer adapter.
