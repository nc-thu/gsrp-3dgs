# Third-party notices

The simulator package itself uses only the Python standard library.

The optional workload adapter interoperates with Speedy-Splat and the
differentiable Gaussian rasterizer. Those upstream projects retain their own
licenses and are not redistributed as part of the public simulator package.
